import {useEffect,useState} from 'react'; import {ShieldCheck,Users as UsersIcon} from 'lucide-react'; import {api} from '../api'; import PageHeader from '../components/PageHeader'; import Badge from '../components/Badge'; import Loading from '../components/Loading';
export default function Users(){const[rows,setRows]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{api.get('/users').then(r=>setRows(r.data.data)).finally(()=>setLoading(false))},[]);return <div><PageHeader eyebrow="ADMIN / ACCESS" title="Users & roles" subtitle="Role-based access keeps each team focused on the work they own." action={<div className="status-banner"><ShieldCheck size={16}/> Protected workspace</div>}/><div className="role-grid"><div><strong>ADMIN</strong><span>Full operational control</span></div><div><strong>SALES</strong><span>CRM + sales challans</span></div><div><strong>WAREHOUSE</strong><span>Products + inventory</span></div><div><strong>ACCOUNTS</strong><span>Document visibility</span></div></div><div className="panel table-panel">{loading?<Loading/>:<div className="table-shell"><table><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Created</th></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td><div className="person-cell"><div className="avatar small">{u.fullName.slice(0,1)}</div><strong>{u.fullName}</strong></div></td><td>{u.email}</td><td><Badge value={u.role}/></td><td>{new Date(u.createdAt).toLocaleDateString('en-IN')}</td></tr>)}</tbody></table></div>}</div><div className="micro-insight"><UsersIcon size={16}/><span>Security demo: every protected API checks the JWT and then validates the user's role before allowing write actions.</span></div></div>}
