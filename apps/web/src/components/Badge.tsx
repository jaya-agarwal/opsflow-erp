export default function Badge({value}:{value:string}){const v=value.toUpperCase(); const c=v.includes('CONFIRM')||v==='ACTIVE'||v==='IN'||v==='OK'? 'badge-success':v.includes('LEAD')||v.includes('DRAFT')||v==='LOW'?'badge-warn':v.includes('CANCEL')||v.includes('INACTIVE')||v==='OUT'?'badge-danger':'badge-neutral'; return <span className={`badge ${c}`}><span className="badge-dot"/>{value}</span>}
